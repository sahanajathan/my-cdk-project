version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install  # Install dependencies including ts-node

  build:
    commands:
      - echo Running CDK deploy...
      - npx cdk deploy  # Deploy using CDK

  post_build:
    commands:
      - echo CDK deploy complete
