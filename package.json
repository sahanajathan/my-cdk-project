{
  "name": "my-cdk-project",
  "version": "0.1.0",
  "type": "module",  // <-- Add this line to enable ES Modules
  "bin": {
    "my-cdk-project": "bin/my-cdk-project.ts"
  },
  "dependencies": {
    "aws-cdk-lib": "2.x.x",  // Use CDK v2
    "constructs": "10.x.x",  // Latest version of constructs
    "ts-node": "^10.0.0",    // Ensure ts-node is available for runtime
    "typescript": "^4.4.0"   // TypeScript version
  },
  "devDependencies": {
    "aws-cdk": "2.x.x"       // CDK toolkit for deploying
  },
  "scripts": {
    "build": "tsc",
    "deploy": "cdk deploy",
    "diff": "cdk diff",
    "synth": "cdk synth"
  }
}
